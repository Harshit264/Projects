const backBtn = document.querySelector(".upbtn")
const contact = document.querySelector(".contact")

backBtn.addEventListener("click", () => {
    window.location.href = "#top"
})

contact.addEventListener("click", () => {
    window.location.href = "#touch"
})